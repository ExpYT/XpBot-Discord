var Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
	const embed = new Discord.RichEmbed();
	.setTitle('XpBot Command Help');
	.setColor('#4caf50');
	.addField('Ping - Will return the ping for the bot, correct use: !ping')
}
 module.exports.help = {
    name: "help"
    }